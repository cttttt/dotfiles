# Should be paired with work in the bashrc that sets TERM to xterm-256color
# where it's not set to screen-256color
#
# e.g.:
#
# if [ $TERM != "screen-256color" ]; then
#     export TERM=xterm-256color
# fi
#
term screen-256color

caption always
bind j focus down
bind k focus up
bind t focus top
bind b focus bottom

# Should be paired with work in ~/.ssh/rc to symlink the SSH_AUTH_SOCK into a
# fixed location under ~/.ssh on every connection:
# 
# # Fix SSH auth socket location so agent forwarding works with screen.
# if test "$SSH_AUTH_SOCK" ; then
#     ln -sf $SSH_AUTH_SOCK ~/.ssh/ssh_auth_sock
# fi
# 
# # Taken from the sshd(8) manpage.
# if read proto cookie && [ -n "$DISPLAY" ]; then
#         if [ `echo $DISPLAY | cut -c1-10` = 'localhost:' ]; then
#                 # X11UseLocalhost=yes
#                 echo add unix:`echo $DISPLAY |
#                     cut -c11-` $proto $cookie
#         else
#                 # X11UseLocalhost=no
#                 echo add $DISPLAY $proto $cookie
#         fi | xauth -q -
# fi
#
setenv SSH_AUTH_SOCK $HOME/.ssh/ssh_auth_sock
